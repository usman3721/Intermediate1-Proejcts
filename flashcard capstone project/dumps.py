# word_in_dict={row.French:row.English for (index,row) in data.iterrows()}
# rand_word=random(word_in_dict["French"])


# rand_word=random.choice(list(word_in_dict.items()))

# phonetic_dict={row.letter:row.code for (index,row) in nato_file.iterrows()}